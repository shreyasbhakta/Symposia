# WordPress origin (REST API + media). No trailing slash.
# Example: WordPress on same domain as frontend
VITE_WORDPRESS_URL=https://symposia.us

# Optional: explicit root production build (same as default unless you use reactpress)
# VITE_DEPLOY_TARGET=root
